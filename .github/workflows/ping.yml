name: Keep Render App Alive

on:
  schedule:
    - cron: "*/10 * * * *"  # Every 10 minutes
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Curl the Render app
        run: curl -s https://essence-tracker.onrender.com > /dev/null